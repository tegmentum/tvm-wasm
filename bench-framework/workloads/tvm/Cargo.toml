[package]
name = "tvm-bench-workload-tvm"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
tvm-guest-rt = { path = "../../../crates/tvm-guest-rt" }

[profile.release]
opt-level = 3
lto = true
strip = true
