[package]
name = "tvm-test-harness"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Reusable benchmarking utilities for TVM workloads (timing, percentiles, statistics, JSON output)."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
